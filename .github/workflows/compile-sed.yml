name: Build BusyBox (sed) to WebAssembly
on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-busybox:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Emscripten
        uses: mymindstorm/setup-emsdk@v14
        with:
          version: latest

      - name: Download BusyBox
        run: |
          wget https://busybox.net/downloads/busybox-1.36.1.tar.bz2
          tar -xjf busybox-1.36.1.tar.bz2
          mv busybox-1.36.1 busybox-src

      - name: Configure BusyBox
        working-directory: ./busybox-src
        run: |
          # 1. Create a seed file to answer the mandatory "choice" questions
          cat <<EOF > ci_config.seed
          CONFIG_SED=y
          CONFIG_STATIC=y
          CONFIG_SH_IS_NONE=y
          CONFIG_BASH_IS_NONE=y
          EOF

          # 2. Apply allnoconfig WITH the seed. 
          # This sets everything to 'n' EXCEPT our seed values.
          make allnoconfig KCONFIG_ALLCONFIG=ci_config.seed
          
          # 3. Finalize without interaction
          make oldconfig KCONFIG_NOTIMESTAMP=1 < /dev/null

          # 4. Fix Emscripten header conflict
          sed -i 's/char BUG_off_t_size_is_misdetected/ \/\/ char BUG_off_t_size_is_misdetected/' include/libbb.h

      - name: Compile Objects
        working-directory: ./busybox-src
        run: |
          # Build only the objects we need. This is fast and ignores top-level failures.
          emmake make -j$(nproc) CC=emcc editors/sed.o libbb/lib.a

      - name: Link to WebAssembly
        run: |
          # 1. Create the wrapper defining the entry point and applet name
          cat <<EOF > wasm_main.c
          const char *applet_name = "sed";
          extern int sed_main(int argc, char **argv);
          int main(int argc, char **argv) {
              return sed_main(argc, argv);
          }
          EOF

          mkdir -p dist
          
          # 2. Link using the .o file directly to ensure sed_main is found
          # We use --whole-archive for libbb to satisfy all internal dependencies
          emcc -O2 \
            wasm_main.c \
            busybox-src/editors/sed.o \
            -Wl,--whole-archive busybox-src/libbb/lib.a -Wl,--no-whole-archive \
            -o dist/busybox.js \
            -s WASM=1 \
            -s MODULARIZE=1 \
            -s EXPORT_NAME=createBusyBox \
            -s ENVIRONMENT=web \
            -s FORCE_FILESYSTEM=1 \
            -s EXPORTED_RUNTIME_METHODS='["FS","callMain"]' \
            -s EXIT_RUNTIME=0 \
            -s INVOKE_RUN=0 \
            -s INITIAL_MEMORY=33554432 \
            -s ALLOW_MEMORY_GROWTH=1

      - name: Commit and Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f dist/busybox.js dist/busybox.wasm
          if git diff --staged --quiet; then
            echo "No changes."
          else
            git commit -m "chore: update compiled busybox-sed wasm"
            git push
          fi
